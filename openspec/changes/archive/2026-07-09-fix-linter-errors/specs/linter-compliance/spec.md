## ADDED Requirements

None. This change is a pure implementation-level maintenance fix (manifest version bump, type annotations, lint compliance). No user-facing capabilities are introduced or modified.
